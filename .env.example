# Local development only. Generate two independent passwords with: openssl rand -hex 24
# Use hexadecimal passwords here so they are also safe in connection URLs.
POSTGRES_PASSWORD=replace-with-generated-owner-password
APP_DB_PASSWORD=replace-with-generated-app-password
